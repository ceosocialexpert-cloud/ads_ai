#!/bin/bash

# Скрипт для вивантаження змін на GitHub
echo "🚀 Починаємо вивантаження змін на GitHub..."

# Перевіряємо чи ми в правильній директорії
if [ ! -d ".git" ]; then
    echo "❌ Помилка: Не знайдено .git директорію. Переконайтесь, що ви в корені проекту."
    exit 1
fi

echo "✅ Перевірка Git статусу..."
git status

echo "📦 Додаємо всі зміни..."
git add .

echo "📝 Створюємо коміт..."
git commit -m "Додано систему управління проектами: збереження ЦА, сторінки проектів, нова архітектура БД"

echo "📤 Відправляємо зміни на GitHub..."
git push origin main

if [ $? -eq 0 ]; then
    echo "✅ Успішно вивантажено на GitHub!"
    echo "🌍 Перегляньте ваш репозиторій на GitHub"
else
    echo "❌ Помилка при відправленні на GitHub"
    echo "Спробуйте виконати:"
    echo "git remote -v"
    echo "git push -u origin main"
fi